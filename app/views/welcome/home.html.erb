<div class="centered" id="welcome" data-anijs="if: click, on: #welcome, do: swing animated">
  <br>
  <h1 id="welcome_header">Welcome to <span>f</span>aridd.com</h1>
  <p class="description" id="welcome_description" data-anijs="do: bounceInRight">The most intelligent drug api on planet earth</p>
</div>

<br><br>
<div class="main feed">
  <div class="ui brown centered two massive statistics">
    <%- models.each do |name, icon| %>
        <%= stat name, icon %>
    <% end %>
  </div>
  <br><br>
<%- if @users %>
  <div class="ui centered cards">
        <%- @users.each do |user| %>
            <%= render  'users/user', user: user %>
        <%- end %>
  </div>
<% end %>
</div>